{
  "id": "methane_butane_saft_vr_mie_against_neqsim",
  "calc": "eos.saft_vr_mie_phase",
  "source": {
    "attribution": "NeqSim master, SystemSAFTVRMie - which builds PhaseSAFTVRMie - run from validation/neqsim/SaftVrMieProbe.java",
    "verification": "verified",
    "notes": "The probe's `v` line is `getVolume()/n` in the class's internal scale, not a molar volume, so the volume is not recorded here; the compressibility and the fugacity coefficients are. **The tolerance is `1e-4`** because this model's last digits are an arithmetic rather than a model: NeqSim takes the `eta` derivatives of `gHS` and of the dispersion by central difference at a relative `1e-5` and the crate reproduces that step, so the two sides are differencing the same nearly-equal energies."
  },
  "derivation": "Every layer reproduces one at a time in `crates/azoth-eos/tests/saft_vr_mie.rs` - the diameter, the chain contact value, `g1`, `g2`, the dispersion, the Helmholtz energy and the pressure. **The departures are the oracle's too, since `#3830`**: the revision this conversion was ported from left the chain contact value's own temperature dependence out of `PhaseSAFTVRMie.dF_HC_SAFTdT`, which put its binary `Hres` 4% from the difference of its own `F`, and that class now reports the difference's value.\n\nThe probe was run as `java -cp .:neqsim-f0c7436.jar SaftVrMieProbe 350 30 methane 0.6 n-butane 0.4`, reading `saft_vr_mie_probe.tsv`; this is its state 1, whose own header line is:\n\n    # methane/n-butane at T=350.000 K, P=30.0000 bara\n\nThe case compares against the values that header's block prints, which are in the capture verbatim.",
  "inputs": {
    "components": ["methane", "n-butane"],
    "T": 350.0,
    "P": 3000000.0,
    "z": [0.6, 0.4],
    "compressed_phase": "vapour"
  },
  "expected": {
    "z_factor": 0.851583764555351,
    "ln_phi": [0.0303536916953227, -0.394262076488891]
  },
  "tolerance": 0.0001
}
