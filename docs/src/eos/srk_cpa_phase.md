<!-- Generated by tools/gen_docs.py from specs/models/eos/srk_cpa_phase.toml. Do not edit by hand. -->

# Soave-Redlich-Kwong CPA phase state

`eos.srk_cpa_phase`

## Source

**Kontogeorgis, G. M. et al. (1996)**

The Soave-Redlich-Kwong cubic with the Wertheim association contribution added to the residual Helmholtz energy, which is what makes the attraction and covolume per-fluid fitted values rather than critical-constant combinations.

DOI: [10.1021/ie9600203](https://doi.org/10.1021/ie9600203)


## What this model is

A **direct** model: a computation over vectors, with no iteration and therefore no algorithm block. It is here because a calculation's inputs are scalars, so a composition vector has nowhere in the calc registry to go - and it is a model rather than a library function so that it goes through the spec-driven pipeline like everything else.

## Inputs

| Name | Unit | Description |
|---|---|---|
| `components` | - | the substances the mixture is made of, by name, resolved against the component databank with the fitted association set and the `cpakij_SRK` column |
| `T` | K | absolute temperature |
| `P` | Pa | absolute pressure |
| `z` | dimensionless | the mixture's mole fractions, checked rather than renormalised |
| `compressed_phase` | liquid / vapour | which root describes the phase wanted. Not a compressibility factor: the association moves the root, so a caller could not supply one without solving the model first. |


## Outputs

| Name | Unit | Description |
|---|---|---|
| `z_factor` | dimensionless | the compressibility factor at the chosen root |
| `ln_phi` | dimensionless | the fugacity coefficients, as logarithms, at the chosen root |
| `h_res` | J/mol | the residual enthalpy, real minus ideal gas at the same temperature, pressure and composition, including the association's `A/(RT) - T d(A/RT)/dT` |
| `s_res` | J/(mol*K) | the residual entropy, real minus ideal gas at the same temperature, pressure and composition |

| Bound | On violation | Why |
|---|---|---|
| `T > 0` | raises | an absolute temperature; zero and below are not states |
| `P > 0` | raises | an absolute pressure; zero and below are not states |

## Assumptions

- a component's fitted set (`aCPA`, `bCPA`, `mCPA`, `kappa_AB`, `epsilon`) comes from the databank with the keycard's overrides applied. A set that is absent or all zero keeps the cubic's own `a` and `b`, which is NeqSim's `|aCPA| > 1e-6` guard.
- the interaction matrix is `cpakij_SRK`, not `KIJPR`: `SystemSrkCPA` runs `setMixingRule(10)`, a CPA rule reading the `cpa` columns of `INTER.csv`. On water/methanol the two differ by a factor of two.
- the association is Wertheim first-order perturbation theory with a Carnahan-Starling radial distribution function: site schemes whose sites bond on a charge-sign product, an Elliott cross rule, and a successive-substitution site-fraction solve refined by Newton.
- **the root is not the cubic's.** The association carries a pressure, so the volume solves `f(Z) = Z - Z/(Z-B) + A Z/((Z+d1 B)(Z+d2 B)) - P_a Z/P = 0` with `P_a = -R T d(A_assoc/(R T))/dV`. The cubic's own root for this fluid at 300 K and 100 bar is 0.15229 where the associating root is 0.10505.
- the liquid root is the *lowest* zero above the covolume and is found by a geometric walk rather than seeded, because at low pressure the substituted cubic has a single real root and the association is what creates the liquid branch.
- no association term is added to the heat-capacity departure, because the association's second temperature derivative is not derived; `cp_res` is therefore absent from the outputs rather than reported from the cubic alone.

## Cases

| Case | Inputs | Expected |
|---|---|---|
| `water_methanol_liquid_against_neqsim` | components = ['water', 'methanol'], T = 300.0, P = 10000000.0, z = [0.6, 0.4], compressed_phase = liquid | z_factor = 0.105050962879418, ln_phi = [-8.02010646183615, -6.14028423012298] |

## References

- Kontogeorgis, G. M.; Voutsas, E. C.; Yakoumis, I. V.; Tassios, D. P. (1996). "An Equation of State for Associating Fluids." Industrial & Engineering Chemistry Research 35(11), 4310-4318. DOI 10.1021/ie9600203
- Michelsen, M. L.; Hendriks, E. M. (2001). "Physical properties from association models." Fluid Phase Equilibria 180(1-2), 165-174. DOI 10.1016/S0378-3812(01)00355-7
- NeqSim master, `PhaseSrkCPA.java` / `ComponentSrkCPA.java` / `CPAMixingRuleHandler.java`, Apache-2.0, commit f0c7436c6923766b1e22957b7075f650600457a7.
