<!-- Generated by tools/gen_docs.py from specs/calcs/eos/heat_of_vaporization.toml. Do not edit by hand. -->

# Heat of vaporisation from NeqSim's correlation

`eos.heat_of_vaporization`

## Equation

$$
\Delta h_{\mathrm{vap}} = 10^{-3}\,c_0\,\left(1 - T_r\right)^{c_1 + c_2 T_r + c_3 T_r^{2}}
$$

In the form the library evaluates:

```python
hov = 1e-3 * c0 * (1 - Tr)**(c1 + c2*Tr + c3*Tr**2)
```

## Source

**NeqSim's `getPureComponentHeatOfVaporization`**

the correlation NeqSim master evaluates in `thermo/component/Component.java`, ported verbatim

## Inputs

| Name | Unit | Description |
|---|---|---|
| `c0` | dimensionless | The first heat-of-vaporisation coefficient, NeqSim's internal scale. |
| `c1` | dimensionless | The exponent's constant term. |
| `c2` | dimensionless | The exponent's linear term. |
| `c3` | dimensionless | The exponent's quadratic term. |
| `Tc` | K | Critical temperature of the pure component. |
| `T` | K | Absolute temperature. |


## Outputs

| Name | Unit | Description |
|---|---|---|
| `hov` | J/mol | The pure-component heat of vaporisation at `T`. |


## Valid range

| Bound | On violation | Why |
|---|---|---|
| `Tc > 0` | raises | `Tc` is the divisor in `Tr = T/Tc`; zero or negative is not a critical temperature. |
| `T > 0` | raises | An absolute temperature; zero and below are not states. |


## Assumptions

These are **not** checked at runtime. They are what the caller has to
satisfy for the result to mean what it says.


- the four coefficients are the raw `heatofvaporizationcoefs1..4` NeqSim ships, in its internal unit; the `1e-3` in the equation is what recovers `J/mol` from them. NOT CHECKED - the coefficients come from the caller.

- `Tr` is reduced against the critical temperature the caller supplies.

- the correlation is a fit of limited range; NeqSim's own data is inconsistent across components (methane's `c0` is three orders of magnitude smaller than n-butane's), which this calc reproduces rather than repairs.

- this calc returns a pure-component property and says nothing about a mixture or a phase.




## Worked example

Source: derived from the equation above

| Input | Value |
|---|---|
| `c0` | 52100000.0 |
| `c1` | 0.32 |
| `c2` | -0.212 |
| `c3` | 0.258 |
| `Tc` | 425.12 |
| `T` | 300.0 |

| Output | Expected |
|---|---|
| `hov` | 36147.57779141032 |

Relative tolerance: `1e-12`

n-butane, c0 = 5.21e7, c1 = 0.32, c2 = -0.212, c3 = 0.258, Tc = 425.12 K, at T = 300 K: Tr = 300 / 425.12 = 0.7056831012420023 1 - Tr = 0.2943168987579977 exponent = 0.32 - 0.212*Tr + 0.258*Tr**2 = 0.32 - 0.1496026256... + 0.128547... = 0.29887625149635627 (1 - Tr)**exponent = 0.6938114739234226 1e-3 * c0 = 52100.0 hov = 52100.0 * 0.6938114739234226 = 36147.57779141032 J/mol


## Tests

| Test | Type | What it does |
|---|---|---|
| `n_butane_worked_example` | `worked_example` | the example above; active |
| `methane_at_a_constant_heat_of_vaporisation` | `reference` | an independent value; active |
| `round_trip_units` | `property` | unit_round_trip; active |


## Implementation

- Python: `azoth.eos.heat_of_vaporization`

- Rust: `azoth_eos::heat_of_vaporization`


## References

- Watson, K. M. (1943). \"Thermodynamics of the Liquid State.\" Ind. Eng. Chem. 35(4), 398-406. DOI 10.1021/ie50400a004.


---

[Back to Equations of state](./index.md)
