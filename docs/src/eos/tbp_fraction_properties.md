<!-- Generated by tools/gen_docs.py from specs/calcs/eos/tbp_fraction_properties.toml. Do not edit by hand. -->

# TBP pseudo-component properties

`eos.tbp_fraction_properties`

## Equation

$$
T_{c} = c_{0}d + c_{1}\ln M + c_{2}M + \frac{c_{3}}{M}, \qquad P_{c} = \exp\left(0.01325 + c_{0} + c_{1}d^{c_{4}} + \frac{c_{2}}{M} + \frac{c_{3}}{M^{2}}\right), \qquad \omega = \frac{3}{7}\frac{\log_{10}(P_{c}/1.01325)}{T_{c}/T_{b} - 1} - 1
$$

In the form the library evaluates:

```python
Pedersen's TBP correlations: mw = molar_mass*1000 (g/mol), d = density/1000 (g/cm3), then

  oil = mw < 1120
  tc  = c_tc[oil][0]*d + c_tc[oil][1]*ln(mw) + c_tc[oil][2]*mw + c_tc[oil][3]/mw                        [K]
  pc  = exp(0.01325 + c_pc[oil][0] + c_pc[oil][1]*d**c_pc[oil][4] + c_pc[oil][2]/mw + c_pc[oil][3]/mw**2)  [bar]
  tb  = 2e-6*mw**3 - 0.0035*mw**2 + 2.4003*mw + 171.74          if mw < 540                             [K]
  tb  = 97.58*mw**0.3323*d**0.04609                            otherwise                                [K]
  m   = c_m[oil][0] + c_m[oil][1]*mw + c_m[oil][2]*d + c_m[oil][3]*mw**2
  omega = 3/7 * log10(pc / 1.01325) / (tc/tb - 1) - 1
```

## Source

**NeqSim 3.20.0 `TBPfractionModel.PedersenTBPModelSRK`**

`calcTC`, `calcPC`, `calcTB`, `calcm` and the base class's `calcAcentricFactor`, as `addTBPfraction` composes them.

## Inputs

| Name | Unit | Description |
|---|---|---|
| `molar_mass` | kg/mol | The cut's molar mass. A pseudo-component has no databank row, so this is what stands in for its identity. |
| `density` | kg/m**3 | The cut's normal liquid density, at 15 C and 1 atm. The correlation takes g/cm3 and this is the same quantity a thousand times larger. |


## Outputs

| Name | Unit | Description |
|---|---|---|
| `tc` | K | Critical temperature. |
| `pc` | Pa | Critical pressure. NeqSim's correlation is written in bar and this reports pascals. |
| `boiling_temperature` | K | Normal boiling point. Two correlations meet at `mw = 540` g/mol and the switch is pinned by a test. |
| `acentric_factor` | dimensionless | Pitzer's acentric factor, from Edmister's three-parameter form rather than from a vapour-pressure fit. |
| `attraction_exponent` | dimensionless | The `m` of the cubic's alpha function, which for a pseudo-component **replaces** the acentric-factor correlation `eos.srk_kappa` makes. NeqSim's `addTBPfraction` sets it on the component's attractive term. |


## Valid range

| Bound | On violation | Why |
|---|---|---|
| `molar_mass > 0` | raises | a molar mass; zero would divide by zero in four coefficients and below it the correlation has no meaning |
| `density > 0` | raises | a normal liquid density; the critical-pressure coefficient raises it to a fractional power |
| `acentric_factor >= -1 and acentric_factor <= 2` | warns `OUT_OF_VALID_RANGE` | **Measured outside this at `mw >= 1120`**: the heavy set gives a boiling point above the critical temperature, so the factor comes out at `-44.6358323245519` at `mw = 1121`, `d = 1.0`. A Pitzer factor outside `[-1, 2]` describes no fluid, so it is reported rather than passed on. |


## Assumptions

These are **not** checked at runtime. They are what the caller has to
satisfy for the result to mean what it says.


- **this is NeqSim's default TBP model, `PedersenSRK`** - `Characterise.TBPFractionModelName`. The class holds eight models and this is the one `addTBPfraction` runs; `RiaziDaubert`, `Lee-Kesler`, `Twu`, `Cavett`, `Standing` and the two heavy-oil variants are carried by name.
- **a cut is two numbers**, a molar mass and a normal liquid density, and everything a cubic needs follows from them. That is what `addTBPfraction` is: it evaluates these correlations and hands the results to a component.
- the correlation is written in **g/mol and g/cm3** and this states SI, so the conversion is at the boundary and the coefficients keep NeqSim's own scale. `pc` is computed in **bar** - its coefficients and `0.01325` (= ln 1.01325) say so - and reported in pascals.
- **the coefficient set switches at `mw = 1120` and the boiling-point correlation at `mw = 540`**, both in g/mol. The two crossings are pinned by tests, because a port that read either threshold in kg/mol would agree at every light cut and diverge only at heavy ones.
- **above `mw = 1120` the acentric factor is unphysical and is returned anyway.** Measured at `mw = 1121`, `d = 1.0`: `tb = 1006.34005439028` K against `tc = 994.658850370848` K, so `omega` is `-44.6358323245519`. The correlation's behaviour, reproduced rather than repaired; the range warns.
- **`racketZ` - the Peneloux shift `addTBPfraction` also fits - is carried**, and is not a function of these two numbers: it takes a flashed reference system. `PhaseSrkEos` never applies it either - its only callers are the density models.
- `calcCriticalVolume`, `calcParachorParameter`, `calcCriticalViscosity` and `calcWatsonCharacterizationFactor` are the same class's neighbours and are carried by name: nothing in the ported families reads them.
- the correlations are fits to petroleum assays, so this is a property of a *cut* and not of a substance, and no databank is involved.



## Worked example

Source: NeqSim 3.20.0, `TBPfractionModel.PedersenTBPModelSRK`, from `validation/neqsim/captures/tbp_fraction_probe.tsv` cut[4]

| Input | Value |
|---|---|
| `molar_mass` | 0.5 |
| `density` | 880.0 |

| Output | Expected |
|---|---|
| `tc` | 891.945256085426 |
| `pc` | 1196293.59599742 |
| `boiling_temperature` | 746.89 |
| `acentric_factor` | 1.36586663262266 |
| `attraction_exponent` | 2.228110216 |

Relative tolerance: `1e-12`

A `C19`-scale cut at `mw = 500.0` g/mol and `d = 0.88` g/cm3 - the two numbers `addTBPfraction` takes - which is below both switches, so the oil set and the cubic boiling point apply: ln(mw) = 6.214608098422191 tc = 163.12*0.88 + 86.052*6.214608098422191 + 0.43475*500.0 - 1877.4/500.0 = 143.5456 + 534.7873345... + 217.375 - 3.7548 = 891.945256085426 K pc = exp(0.01325 - 0.13408 + 2.5019*0.88**1.0 + 208.46/500.0 - 3987.2/500.0**2) = exp(0.01325 - 0.13408 + 2.201672 + 0.41692 - 0.0159488) = 11.9629359599742 bar tb = 2e-6*500.0**3 - 0.0035*500.0**2 + 2.4003*500.0 + 171.74 = 746.890000000000 K m = 0.7431 + 0.0048122*500.0 + 0.0096707*0.88 - 3.7184e-6*500.0**2 = 2.22811021600000 omega = 3/7*log10(11.9629359599742/1.01325)/(891.945256085426/746.89 - 1) - 1 = 3/7*1.0717984/0.1942125 - 1 = 1.36586663262266 The capture's rows are those five numbers.


## Tests

| Test | Type | What it does |
|---|---|---|
| `c19_worked_example` | `worked_example` | the example above; active |
| `the_boiling_point_switch_at_mw_540` | `reference` | an independent value; active |
| `the_coefficient_set_switch_at_mw_1120` | `reference` | an independent value; active |
| `round_trip_units` | `property` | unit_round_trip; active |


## Implementation

- Python: `azoth.eos.tbp_fraction_properties`

- Rust: `azoth_eos::tbp_fraction_properties`


## References

- Pedersen, K. S.; Thomassen, P.; Fredenslund, A. (1989). "Characterization of gas condensate mixtures." Advances in Thermodynamics 1, 137-152.
- Pedersen, K. S.; Christensen, P. L. (2007). "Phase Behavior of Petroleum Reservoir Fluids.", CRC Press.
- NeqSim - https://github.com/equinor/neqsim - Apache-2.0. `TBPfractionModel.PedersenTBPModelSRK` is the port source.


---

[Back to Equations of state](./index.md)
