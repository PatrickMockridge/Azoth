<!-- Generated by tools/gen_docs.py from specs/models/process/distillation_column.toml. Do not edit by hand. -->

# Distillation column

`process.distillation_column`

## Source

**NeqSim process/equipment/distillation/DistillationColumn.java**

Its `init` and its `solveSequential` under the default `DIRECT_SUBSTITUTION`: the feed-stage flash, the linear temperature seed, the two links, and the sweeps. The other nine solving strategies are refused by name.


## Algorithm

A model rather than a calculation: what this page pins down is the procedure,
not an equation, and both implementations read it from here.

| Setting | Value |
|---|---|
| Scheme | `sequential_substitution_sweeps` |
| Convergence | `absolute` |
| Tolerance | `1e-06` |
| Max iterations | `200` |
| Initialisation | `linear_temperature_profile` |

## Inputs

| Name | Unit | Description |
|---|---|---|
| `components` | - | the feed's substances, by name, resolved against the component databank. The order is the order of `feed_z`. |
| `feed_n` | mol/s | the feed's molar flow. |
| `feed_z` | dimensionless | the feed composition. |
| `feed_p` | Pa | the feed's pressure, which is its own - the column's tray pressures come from `bottom_pressure` and `top_pressure`. |
| `feed_t` | K | the feed's temperature, which with its pressure and composition fixes its enthalpy. |
| `number_of_stages` | dimensionless | the theoretical trays between the ends, which is the palette's own parameter; a reboiler or a condenser adds one stage each. |
| `feed_stage` | dimensionless | the stage the feed enters, 0-based over the trays including the ends - so stage 0 is the reboiler where there is one, and the last stage is the condenser. |
| `has_reboiler` | - | whether the column has a reboiler at stage 0. |
| `has_condenser` | - | whether the column has a condenser at the top stage. |
| `top_pressure` | Pa | the pressure at the top stage. |
| `bottom_pressure` | Pa | the pressure at stage 0. The two fix the profile the tray pressures interpolate. |
| `reboiler_temperature` | K | *Optional.* the reboiler's temperature, which pins the bottom tray's flash. **Absent means no pin**, and the end's flash is then at its own enthalpy - which is what `setReboilerTemperature` not having been called does, and what makes a duty specification reachable at all. |
| `condenser_temperature` | K | *Optional.* the condenser's temperature, which pins the top tray's flash. Absent as the reboiler's is. **A pin wins over a directly-applied duty**: the tray tests its stated outlet temperature first and takes a `TPflash` there, so a heat input a duty specification wrote is never read. |
| `temperature_tolerance` | K | the convergence gate on the mean tray-temperature change between iterations. Where a solve stops is what its answer is, so this is a measurement and not a knob. |
| `max_iterations` | dimensionless | the iteration cap. A solve that reaches it without meeting the gate is refused, with its residuals named. |
| `murphree_efficiency` | dimensionless | *Optional.* **not ported**: the column-wide Murphree tray efficiency, which `SimpleTray.setMurphreeEfficiency` applies and this model refuses. Omitted means the ideal stage, which is the class's own default of one. |
| `solver_type` | direct_substitution / damped_substitution / inside_out / matrix_inside_out / wegstein / sum_rates / newton / naphtali_sandholm / mesh_residual / auto | *Optional.* **only `direct_substitution` is ported**, which is the class's own default. The rest are refused by name - `NaphtaliSandholmSolver`, `ColumnSolverFactory`'s inside-out family - and `auto` is a ladder rather than one method. |
| `top_specification_type` | product_purity / reflux_ratio / component_recovery / product_flow_rate / duty | *Optional.* the top product's degree of freedom, which `ColumnSpecification` carries. Omitted, the top is pinned by temperature - the class's other route. **The end is the parameter's name and not a value**, because the class holds exactly two specifications. |
| `top_specification_target` | dimensionless | *Optional.* the target value, in the unit its type implies: dimensionless for a purity, a recovery or a ratio, **mol/hr** for a flow rate, W for a duty - which is `ColumnSpecification.defaultTargetUnit`'s own convention. |
| `top_specification_component` | - | *Optional.* the component a purity or a recovery constrains; the class's own `validate` refuses such a specification without one, and the other three types do not read it. |
| `bottom_specification_type` | product_purity / reflux_ratio / component_recovery / product_flow_rate / duty | *Optional.* the bottom product's degree of freedom, which `ColumnSpecification` carries. Omitted, the bottom is pinned by temperature - the class's other route. **The end is the parameter's name and not a value**, because the class holds exactly two specifications. |
| `bottom_specification_target` | dimensionless | *Optional.* the target value, in the unit its type implies, as the top specification's is. |
| `bottom_specification_component` | - | *Optional.* the component a purity or a recovery constrains; unread by the other three types. |


## Outputs

| Name | Unit | Description |
|---|---|---|
| `tray_temperature` | K | the solved temperature profile, which is what a column's answer is rather than a scalar. |
| `tray_pressure` | Pa | each tray's pressure, which is the linear profile between `bottom_pressure` and `top_pressure`. |
| `tray_gas_n` | mol/s | the vapour leaving each tray, which is its upward traffic. |
| `tray_liquid_n` | mol/s | the liquid leaving each tray, which is its downward traffic. |
| `distillate_n` | mol/s | the overhead product, which is the condenser's own outlet. |
| `distillate_z` | dimensionless | the distillate's composition. |
| `distillate_p` | Pa | the distillate's pressure, which is `top_pressure` where there is a condenser. |
| `distillate_t` | K | the distillate's temperature, which is the condenser's own. |
| `distillate_h` | J/mol | the distillate's molar enthalpy at its own state. |
| `bottoms_n` | mol/s | the bottom product, which is the reboiler's liquid. |
| `bottoms_z` | dimensionless | the bottoms' composition. |
| `bottoms_p` | Pa | the bottoms' pressure, which is `bottom_pressure` where there is a reboiler. |
| `bottoms_t` | K | the bottoms' temperature, which is the reboiler's own. |
| `bottoms_h` | J/mol | the bottoms' molar enthalpy at its own state. |
| `condenser_duty` | W | the condenser's duty: its products' enthalpy less the vapour's that came in, which is negative for a condenser. |
| `reboiler_duty` | W | the reboiler's duty: its boilup and its bottoms less the downcomer's enthalpy. |
| `iterations` | dimensionless | the iterations taken. **One apart from NeqSim's on the captured row** - 14 against 15 - because where a solve stops is where its answer is. |
| `temperature_residual` | dimensionless | the mean tray-temperature change at the last iteration, in K - the gate the solve was held to. |
| `mass_residual` | dimensionless | the products' worst component imbalance against the feed, relative. **The closure and not the class's MESH norm** - see the assumptions. |
| `energy_residual` | dimensionless | `|H_feed + duties - H_products| / |H_feed|`, the enthalpy closure. As the mass residual, not the class's norm. |

| Bound | On violation | Why |
|---|---|---|
| `number_of_stages >= 1` | raises | a column with no stages between its ends is not a column |
| `top_pressure > 0` | raises | an absolute pressure |
| `bottom_pressure > 0` | raises | an absolute pressure |
| `temperature_tolerance > 0` | raises | a convergence tolerance is positive, and zero is a solve that never stops |
| `feed_t > 0` | raises | an absolute temperature |

## Assumptions

- **the solve is `DIRECT_SUBSTITUTION`**, which is `DistillationColumn`'s own default: `init` flashes the feed stage, seeds a linear temperature profile and links the trays, then the sweeps run each tray from its neighbours' outlets until the mean tray-temperature change is under the tolerance.
- **the relaxation is the class's, and its controller reads one term of three.** It damps on a combined residual carrying its scaled temperature, mass and energy terms, and the others are `ColumnMeshResidualEvaluator`'s - a MESH norm this port does not carry.
- **that matters only where a solve struggles.** On the captured binary row both libraries keep a relaxation of one and reach the same fixed point.
- **the linear seed enters through the inlets.** `SimpleTray.init()` re-states every inlet stream's temperature to the tray's before its first run, which is how the profile reaches the trays at all.
- **the external feed is restored afterwards**, which is why the sweeps see the feed's own state and not the seeded one.
- **the reboiler's `init` inlet is the feed stage's liquid**, not the first tray's - `init` links it that way and the sweeps then replace it with the first tray's. The two differ only during the seeding.
- **the ends are pinned by temperature**: `setCondenserTemperature` reaches the tray's own `outTemperature`, so its flash is a `TPflash` there rather than at the mixed enthalpy. A middle tray has no pin and flashes at its enthalpy.
- **pressures run linearly** from `bottom_pressure` at stage 0 to `top_pressure` at the top stage, which is the class's own rule and is why the two are both declared.
- **`number_of_stages` counts the trays between the ends**, so a reboiler or a condenser adds one stage each: the stage a feed enters is 0-based over the trays *including* them, and stage 0 is the reboiler where there is one.
- **the mass and energy residuals are the closure, not the class's.** They are the products' worst component imbalance against the feed, and the enthalpy balance; `getLastMassResidual` is a norm over the MESH equations. The capture prints both, and they are not comparable.
- **the product specifications are `ColumnSpecification`'s five types**, and the class splits them in two: a **reflux ratio** and a **duty** are written straight onto the end, and the other three by an **outer secant on the end's temperature**.
- **a specification's location follows its slot.** `validateColumnSpecification` refuses a `TOP` spec handed to `setBottomSpecification`, so the location is a spelling of the slot; both ends are reachable, the top's secant on the condenser and the bottom's on the reboiler.
- **that secant takes two guesses five kelvin apart**, then steps capped at 50 K and clamped to `[100, 1000]` K, and it converges when the product's value is within `1e-4` of the target.
- **there is no specification homotopy here, and that is a measurement.** `getEffectiveSpecificationHomotopySteps` returns three staged targets only when the solver is `AUTO`, and the field it otherwise reads is one - so the continuation the class carries belongs to the solver this port refuses.
- **a pin wins over a directly-applied duty.** The tray tests its stated outlet temperature first and takes a `TPflash` there, so a heat input a `DUTY` wrote is never read: the capture's inert row reports `-21323.04` W for a target of `-20000`.
- **nothing reports that gap**: a `DUTY` is not *adjusted*, so its residual is zero by construction - a zero that means "not checked" rather than "satisfied".
- **a reflux-ratio specification is not reproduced, and the reason is measured.** The end's `PVrefluxflash` searches for the state whose vapour fraction is `1/(1 + ratio)`; NeqSim lands on `402.24` K, beyond the dew point of a 19 bara mixture that is 82 per cent methane.
- **this port's flash refuses to converge there** rather than landing on a state its own equations do not place.
- **`murphree_efficiency` is declared and refused.** The palette declares it because it is the machine's; its arithmetic is not ported, and `SimpleTray.setMurphreeEfficiency` would close it. A refused parameter is refused by name rather than answered with the ideal stage.
- **`solver_type` is refused for everything but `direct_substitution`**, which is the class's own default. The other nine are `NaphtaliSandholmSolver`, `ColumnSolverFactory`'s inside-out family and the rest.
- **`AUTO` is a ladder and not one method** - the parking note's finding - so it is refused with them rather than silently resolved.
- **the fluid is PR with the classic mixing rule**, because `Stream::mixture()` resolves `databank::mixture_of(names, Cubic::Pr, None)` and has no other route.
- **NeqSim's own deethanizer state is written on SRK**, and on PR it does not converge in NeqSim at all - the capture keeps that as evidence.

## Cases

| Case | Inputs | Expected |
|---|---|---|
| `binary_rigorous` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, condenser_temperature = 253.14999999999998, temperature_tolerance = 1e-06, max_iterations = 200 | tray_temperature = [373.15, 336.15382381733946, 303.0710539116486, 302.1225394582587, 296.84208939635556, 253.14999999999998], tray_pressure = [2000000.0, 1980000.0, 1960000.0000000002, 1939999.9999999998, 1920000.0, 1900000.0], tray_gas_n = [1.373278181743518, 0.5259031551756898, 4.5761815164886315, 4.558039582740156, 4.451759798394696, 3.7914997294607584], tray_liquid_n = [3.6992043068302056, 5.072482507910388, 4.225107543947319, 0.784681868969297, 0.7665399361896209, 0.6602601517057779], distillate_n = 3.7914997294607584, distillate_z = [0.9659099052324753, 0.03409009476752466], distillate_p = 1900000.0, distillate_t = 253.14999999999998, distillate_h = -1258.451319893305, bottoms_n = 3.6992043068302056, bottoms_z = [0.02246562977682579, 0.9775343702231742], bottoms_p = 2000000.0, bottoms_t = 373.15, bottoms_h = -6818.722955062241, condenser_duty = -21323.042789303567, reboiler_duty = 47786.58389381015 |
| `binary_looser_gate` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, condenser_temperature = 253.14999999999998, temperature_tolerance = 0.01, max_iterations = 200 | tray_temperature = [373.15, 336.1461469511687, 303.0684737943885, 302.1208351248661, 296.84135336194566, 253.14999999999998], tray_pressure = [2000000.0, 1980000.0, 1960000.0000000002, 1939999.9999999998, 1920000.0, 1900000.0], tray_gas_n = [1.3726897093823904, 0.5256141980211493, 4.575940529466956, 4.557821766053063, 4.451569165299028, 3.7913503170929044], tray_liquid_n = [3.6993537191980588, 5.073058247556919, 4.224818011224946, 0.7844403063797876, 0.7663078404755527, 0.6600629788953105], distillate_n = 3.7913503170929044, distillate_z = [0.965932409618382, 0.034067590381618044], distillate_p = 1900000.0, distillate_t = 253.14999999999998, distillate_h = -1258.3755519082827, bottoms_n = 3.6993537191980588, bottoms_z = [0.022480648502718616, 0.9775193514972814], bottoms_p = 2000000.0, bottoms_t = 373.15, bottoms_h = -6817.385259791932, condenser_duty = -21316.79905968026, reboiler_duty = 47785.789477065184 |
| `spec_top_purity` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, top_specification_type = product_purity, top_specification_target = 0.98, top_specification_component = methane, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, condenser_temperature = 240.99878449426998, temperature_tolerance = 1e-06, max_iterations = 200 | tray_temperature = [373.15, 336.15983204948054, 303.0871778633712, 301.8656700973966, 295.0566533964114, 240.99878449426998], tray_pressure = [2000000.0, 1980000.0, 1960000.0000000002, 1939999.9999999998, 1920000.0, 1900000.0], tray_gas_n = [1.3937215237371428, 0.5337683582925649, 4.582334077049384, 4.557259361528284, 4.430929979123594, 3.735722909260181], tray_liquid_n = [3.7549811270307836, 5.148702623297136, 4.288749674102845, 0.8466113565687006, 0.8215366417283955, 0.6952072544098105], distillate_n = 3.735722909260181, distillate_z = [0.9799961726346504, 0.02000382736534954], distillate_p = 1900000.0, distillate_t = 240.99878449426998, distillate_h = -1681.8863468753977, bottoms_n = 3.7549811270307836, bottoms_z = [0.022465629776824245, 0.9775343702231757], bottoms_p = 2000000.0, bottoms_t = 373.15, bottoms_h = -6818.722955062256, condenser_duty = -23926.07136724649, reboiler_duty = 48497.640126440936 |
| `spec_top_flow_rate` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, top_specification_type = product_flow_rate, top_specification_target = 14000.0, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, condenser_temperature = 266.5461555978285, temperature_tolerance = 1e-06, max_iterations = 200 | tray_temperature = [373.15, 336.1347370150918, 303.0198220069367, 302.3671996669844, 298.7999854167456, 266.5461555978285], tray_pressure = [2000000.0, 1980000.0, 1960000.0000000002, 1939999.9999999998, 1920000.0, 1900000.0], tray_gas_n = [1.3379253252061571, 0.5122539474525568, 4.56495192985594, 4.554476416428232, 4.4786755019042115, 3.888888888888851], tray_liquid_n = [3.6018151474021125, 4.939740466283366, 4.114069050562915, 0.6760629966753332, 0.6655874828308178, 0.58978656836433], distillate_n = 3.888888888888851, distillate_z = [0.9422832679729576, 0.0577167320270424], distillate_p = 1900000.0, distillate_t = 266.5461555978285, distillate_h = -786.2300109413297, bottoms_n = 3.6018151474021125, bottoms_z = [0.022465629776832016, 0.977534370223168], bottoms_p = 2000000.0, bottoms_t = 373.15, bottoms_h = -6818.722955062148, condenser_duty = -17715.90123296374, reboiler_duty = 46557.369140281364 |
| `spec_top_duty` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, top_specification_type = duty, top_specification_target = -20000.0, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, temperature_tolerance = 1e-06, max_iterations = 200 | tray_temperature = [373.15, 336.1481573490616, 303.0558449518993, 302.22736201222193, 297.6280388169412, 258.4848270243094], tray_pressure = [2000000.0, 1980000.0, 1960000.0000000002, 1939999.9999999998, 1920000.0, 1900000.0], tray_gas_n = [1.3610998901298772, 0.5212059783418859, 4.572374437187225, 4.5573227720539204, 4.46226677208499, 3.8249569643784094], tray_liquid_n = [3.6657470719125547, 5.026847013309145, 4.186953121738792, 0.7474175442931685, 0.7323658806490708, 0.6373098820904213], distillate_n = 3.8249569643784094, distillate_z = [0.957657516563423, 0.04234248343657693], distillate_p = 1900000.0, distillate_t = 258.4848270243094, distillate_h = -1071.905280771116, bottoms_n = 3.6657470719125547, bottoms_z = [0.022465629776827367, 0.9775343702231726], bottoms_p = 2000000.0, bottoms_t = 373.15, bottoms_h = -6818.722955062222, condenser_duty = -20000.000040496427, reboiler_duty = 47363.104656687734 |
| `spec_bottom_purity` | components = ['methane', 'n-butane'], feed_n = 7.490704036290964, feed_z = [0.49999999999999994, 0.49999999999999994], feed_p = 2000000.0, feed_t = 300.0, number_of_stages = 4, feed_stage = 2, has_reboiler = True, has_condenser = True, top_pressure = 1900000.0, bottom_pressure = 2000000.0, reboiler_temperature = 373.15, condenser_temperature = 253.14999999999998, temperature_tolerance = 1e-06, max_iterations = 200, bottom_specification_type = product_purity, bottom_specification_target = 0.98, bottom_specification_component = n-butane | tray_temperature = [374.80351966552143, 340.24522101778484, 303.9618619617998, 302.98116031387553, 297.55732208927344, 253.14999999999998], tray_pressure = [2000000.0, 1980000.0, 1960000.0, 1940000.0, 1920000.0, 1900000.0], tray_gas_n = [1.5542881400873245, 0.581324277281251, 4.616293976185091, 4.59672964629458, 4.4839880160822405, 3.800889917723617], tray_liquid_n = [3.6898141185673485, 5.244102513025328, 4.271139175611518, 0.8154049146237493, 0.7958403845390866, 0.683098407164445], distillate_n = 3.800889917723617, distillate_z = [0.9659099052324753, 0.034090094767524684], distillate_p = 1900000.0, distillate_t = 253.14999999999998, distillate_h = -1258.4513198933046, bottoms_n = 3.6898141185673485, bottoms_z = [0.020064767825946687, 0.9799352321740532], bottoms_p = 2000000.0, bottoms_t = 374.80351966552143, bottoms_h = -6536.600986014013, condenser_duty = -22004.102481235368, reboiler_duty = 49560.82240422304 |

## References

- NeqSim - https://github.com/equinor/neqsim - Apache-2.0. `process/equipment/distillation/DistillationColumn.java` is the port source, through `init` and `solveSequential`; the stage is `SimpleTray.java`, ported as `crates/azoth-process/src/column/tray.rs`.
- `validation/neqsim/captures/process_column.tsv` - eleven rows: NeqSim's own deethanizer taken to 80 and to 200 iterations, the binary column at a `1e-6` and at a `1e-2` gate, one row per specification type, and the bottom location on the same purity its top case asks for.
