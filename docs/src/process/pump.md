<!-- Generated by tools/gen_docs.py from specs/models/process/pump.toml. Do not edit by hand. -->

# Pump

`process.pump`

## Source

**NeqSim process/equipment/pump/Pump.java**

Its steady-state `run`: an isentropic flash to the outlet pressure at the inlet entropy, the step divided by the isentropic efficiency, and the outlet temperature solved from the shifted enthalpy by a second flash.


## What this model is

A **direct** model: a computation over vectors, with no iteration and therefore no algorithm block. It is here because a calculation's inputs are scalars, so a composition vector has nowhere in the calc registry to go - and it is a model rather than a library function so that it goes through the spec-driven pipeline like everything else.

## Inputs

| Name | Unit | Description |
|---|---|---|
| `components` | - | the fluid's substances, by name, resolved against the component databank. Every port of a unit operation carries the same fluid, so this is declared once. |
| `inlet_n` | mol/s | the inlet's molar flow. A pump moves what it is given, so this is also the outlet's - the kernel passes it through rather than recomputing it. |
| `inlet_z` | dimensionless | the inlet composition, summing to one. |
| `inlet_p` | Pa | the inlet pressure, which with `inlet_t` and `inlet_z` fixes the inlet state and its entropy. |
| `inlet_t` | K | the inlet temperature, which with `inlet_p` and `inlet_z` fixes the inlet enthalpy. |
| `outlet_pressure` | Pa | the pressure the pump raises the stream to, which is `unit_ops.pump`'s own parameter. |
| `isentropic_efficiency` | dimensionless | the pump's isentropic efficiency, in `(0, 1]`. The isentropic head divides by it, so an efficiency of one is the reversible limit - and the captured second row is exactly that. |


## Outputs

| Name | Unit | Description |
|---|---|---|
| `outlet_n` | mol/s | the outlet molar flow, which is the inlet's: a pump adds work and no moles. |
| `outlet_z` | dimensionless | the outlet composition, which is the inlet's. |
| `outlet_p` | Pa | the outlet pressure, which is `outlet_pressure` rather than something the pump solves for. |
| `outlet_t` | K | the outlet temperature, solved from the shifted enthalpy at the outlet pressure. |
| `outlet_h` | J/mol | the outlet molar enthalpy: the inlet's plus `(H(P_out, s_in) - H(P_in)) / eta`. Reported because a machine's whole effect on the stream is this and the temperature it implies. |

| Bound | On violation | Why |
|---|---|---|
| `isentropic_efficiency > 0 and isentropic_efficiency <= 1` | raises | the reversible work divides by it, so zero has no answer and above one would recover more work than isentropic |
| `inlet_t > 0` | raises | an absolute temperature |

## Assumptions

- **an inlet carries four of the record's five fields and an outlet all five.** `h` is a state function of `(T, P, z)` - the same flash decides it - so accepting one would let a case hand over a state that does not exist, while an outlet reports the `h` it reached because a machine changes it.
- **this is `unit_ops.pump`'s arithmetic, not a second one.** The kernel in `azoth_process::kernels` is what the flowsheet calls and what this wraps; the only thing added here is the declared input list and the result type.
- **the fluid is PR with the classic mixing rule**, because `Stream::mixture()` resolves `databank::mixture_of(names, Cubic::Pr, None)` and has no other route. A case on SRK or a non-classic rule is not a case this can be held to.
- **the work is an isentropic head from a flash, not `v (P_out - P_in)`.** NeqSim clones the fluid to the outlet pressure and runs a `PSflash` at the inlet entropy, because `calculateAsCompressor` defaults to `true`; the incompressible form is its linearisation.
- **the outlet temperature is solved, not assumed isothermal.** The work raises the enthalpy and `Stream::from_ph` finds the temperature at which the mixture carries it.
- NeqSim's `Pump` also carries a `runTransient` path, a mechanical design and a performance curve. **None is ported**: this is the steady-state arithmetic, which is what the palette's `outlet_pressure` and `isentropic_efficiency` reach.

## Cases

| Case | Inputs | Expected |
|---|---|---|
| `butane_5_to_20_bara_at_250_k` | components = ['n-butane'], inlet_n = 1.0, inlet_z = [1.0], inlet_p = 500000.0, inlet_t = 250.0, outlet_pressure = 2000000.0, isentropic_efficiency = 0.75 | outlet_n = 1.0, outlet_z = [1.0], outlet_p = 2000000.0, outlet_t = 250.76214168277508, outlet_h = -25382.694279985513 |
| `the_work_divides_by_the_efficiency` | components = ['n-butane'], inlet_n = 1.0, inlet_z = [1.0], inlet_p = 500000.0, inlet_t = 250.0, outlet_pressure = 2000000.0, isentropic_efficiency = 1.0 | outlet_n = 1.0, outlet_z = [1.0], outlet_p = 2000000.0, outlet_t = 250.40558913594867, outlet_h = -25426.73406996356 |

## References

- NeqSim - https://github.com/equinor/neqsim - Apache-2.0. `process/equipment/pump/Pump.java` is the port source, through its `run`.
- Smith, J. M.; Van Ness, H. C.; Abbott, M. M. (2005). "Introduction to Chemical Engineering Thermodynamics.", 7th ed., McGraw-Hill. The incompressible-liquid work `v(P2 - P1)` and the isentropic efficiency that divides it.
