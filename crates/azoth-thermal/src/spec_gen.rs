//! GENERATED FILE - DO NOT EDIT BY HAND.
//!
//! Generated by `tools/gen_registry.py` from:
//!
//!   - specs/calcs/thermal/conduction_plane_wall.toml
//!
//! Tables for the `thermal` namespace. Every namespace has its own generated
//! file, because a crate is the unit of compilation and a calculation must be able
//! to read its own bounds without depending on another namespace's crate. The
//! types these tables are built from are shared - see `azoth_core::spec`.
//!
//! These tables are what make the specs authoritative at runtime rather than
//! merely descriptive. Each calc reads its own range checks from here, so a
//! bound changed in a spec file changes the code's behaviour with no second
//! edit - and `cargo test` fails if the two ever disagree.
use azoth_core::{Band, CalcSpec, RangeCheck, Severity, SpecCheck, TestCase, WarningCode};

/// Registry entry for `thermal.conduction_plane_wall`.
static CONDUCTION_PLANE_WALL_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "k",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "Thermal conductivity is strictly positive for any real material.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "A",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "an area is positive by definition; zero area carries no heat and would make `q` zero regardless of the temperature difference",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "L",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "`L` is a divisor, so `L = 0` is singular - and physically it is the infinite-heat-flow limit of a zero-thickness wall, which is not a state the steady plane-wall model describes.",
        },
    },
];

static CONDUCTION_PLANE_WALL_TESTS: &[TestCase] = &[
    TestCase {
        id: "brick_wall_different_numbers",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("k", 0.7), ("A", 10.0), ("dT", 20.0), ("L", 0.2)],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 700.0)],
        expected_vectors: &[],
        expected_strings: &[],
    },
    TestCase {
        id: "sign_follows_the_temperature_difference",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("k", 45.0), ("A", 2.0), ("dT", -30.0), ("L", 0.05)],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", -54000.0)],
        expected_vectors: &[],
        expected_strings: &[],
    },
    TestCase {
        id: "monotonic",
        kind: "property",
        property: Some("monotonic"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
        expected_strings: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
        expected_strings: &[],
    },
];

/// Registered spec for `thermal.conduction_plane_wall`.
///
/// Public and addressable directly, so a calc can hold `&CONDUCTION_PLANE_WALL_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static CONDUCTION_PLANE_WALL_SPEC: CalcSpec = CalcSpec {
    id: "thermal.conduction_plane_wall",
    checks: CONDUCTION_PLANE_WALL_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "steel_plate_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("k", 45.0), ("A", 2.0), ("dT", 30.0), ("L", 0.05)],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 54000.0)],
        expected_vectors: &[],
        expected_strings: &[],
    },
    tests: CONDUCTION_PLANE_WALL_TESTS,
};

/// Every calculation in the registry, sorted by id.
static ALL_SPECS: &[&CalcSpec] = &[&CONDUCTION_PLANE_WALL_SPEC];

/// All specs, in a stable order.
#[must_use]
pub fn specs() -> &'static [&'static CalcSpec] {
    ALL_SPECS
}

/// Look up a spec by id.
#[must_use]
pub fn spec(id: &str) -> Option<&'static CalcSpec> {
    ALL_SPECS.iter().copied().find(|s| s.id == id)
}
