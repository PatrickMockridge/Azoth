//! GENERATED FILE - DO NOT EDIT BY HAND.
//!
//! Generated by `tools/gen_registry.py` from:
//!
//!   - specs/calcs/reactions/equilibrium_constant.toml
//!
//! Tables for the `reactions` namespace. Every namespace has its own generated
//! file, because a crate is the unit of compilation and a calculation must be able
//! to read its own bounds without depending on another namespace's crate. The
//! types these tables are built from are shared - see `azoth_core::spec`.
//!
//! These tables are what make the specs authoritative at runtime rather than
//! merely descriptive. Each calc reads its own range checks from here, so a
//! bound changed in a spec file changes the code's behaviour with no second
//! edit - and `cargo test` fails if the two ever disagree.
use azoth_core::{Band, CalcSpec, RangeCheck, Severity, SpecCheck, TestCase, WarningCode};

/// Registry entry for `reactions.equilibrium_constant`.
static EQUILIBRIUM_CONSTANT_CHECKS: &[SpecCheck] = &[SpecCheck {
    on_input: true,
    check: RangeCheck {
        quantity: "T",
        min: Some(0.0),
        min_inclusive: false,
        max: None,
        max_inclusive: true,
        equals: None,
        band: Band::Outside,
        severity: Severity::Error,
        code: WarningCode::OutOfValidRange,
        rationale: "an absolute temperature, and the correlation takes its logarithm",
    },
}];

static EQUILIBRIUM_CONSTANT_TESTS: &[TestCase] = &[
    TestCase {
        id: "the_same_row_far_from_the_reference_temperature",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("T", 423.15)],
        flags: &[],
        lists: &[],
        strings: &[("reaction", "CO2water"), ("source", "standard")],
        vectors: &[],
        matrices: &[],
        expected: &[
            ("ln_k", -15.695936068561252),
            ("k", 1.525251908618576e-07),
            ("reaction_heat", -31791.96673583765),
        ],
        expected_vectors: &[],
        expected_strings: &[],
    },
    TestCase {
        id: "the_pitzer_source_is_a_different_standard_state",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("T", 423.15)],
        flags: &[],
        lists: &[],
        strings: &[("reaction", "CO2water"), ("source", "pitzer")],
        vectors: &[],
        matrices: &[],
        expected: &[
            ("ln_k", -15.483910703072212),
            ("k", 1.8854847466869188e-07),
            ("reaction_heat", -22651.697394518887),
        ],
        expected_vectors: &[],
        expected_strings: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        flags: &[],
        lists: &[],
        strings: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
        expected_strings: &[],
    },
];

/// Registered spec for `reactions.equilibrium_constant`.
///
/// Public and addressable directly, so a calc can hold `&EQUILIBRIUM_CONSTANT_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static EQUILIBRIUM_CONSTANT_SPEC: CalcSpec = CalcSpec {
    id: "reactions.equilibrium_constant",
    checks: EQUILIBRIUM_CONSTANT_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "the_co2_water_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("T", 298.15)],
        flags: &[],
        lists: &[],
        strings: &[("reaction", "CO2water"), ("source", "standard")],
        vectors: &[],
        matrices: &[],
        expected: &[
            ("ln_k", -14.633690407708798),
            ("k", 4.412340363006617e-07),
            ("reaction_heat", 9199.128566216192),
        ],
        expected_vectors: &[],
        expected_strings: &[],
    },
    tests: EQUILIBRIUM_CONSTANT_TESTS,
};

/// Every calculation in the registry, sorted by id.
static ALL_SPECS: &[&CalcSpec] = &[&EQUILIBRIUM_CONSTANT_SPEC];

/// All specs, in a stable order.
#[must_use]
pub fn specs() -> &'static [&'static CalcSpec] {
    ALL_SPECS
}

/// Look up a spec by id.
#[must_use]
pub fn spec(id: &str) -> Option<&'static CalcSpec> {
    ALL_SPECS.iter().copied().find(|s| s.id == id)
}
