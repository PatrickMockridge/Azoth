[package]
name = "azoth-reactions"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Chemical reaction equilibrium: element balances, reference potentials, constrained Gibbs minimisation"

[dependencies]
azoth-core.workspace = true
csv.workspace = true
serde.workspace = true
toml.workspace = true
# `reactions.reactive_tp_flash` needs a fugacity coefficient at a trial composition, and the
# model is where the equation of state is chosen; every other id in this crate takes `ln phi`
# as a closure and carries no EoS of its own.
azoth-eos.workspace = true

[dev-dependencies]
azoth-test-support.workspace = true
# Only the tests: the crate itself is dimensionless and needs no units library.
uom.workspace = true

[lints]
workspace = true
