//! GENERATED FILE - DO NOT EDIT BY HAND.
//!
//! Generated by `tools/gen_registry.py` from:
//!
//!   - specs/calcs/hydraulics/choked_flow_area.yaml
//!   - specs/calcs/hydraulics/control_valve_cv.yaml
//!   - specs/calcs/hydraulics/crane_k_factors.yaml
//!   - specs/calcs/hydraulics/darcy_weisbach.yaml
//!   - specs/calcs/hydraulics/friction_factor_colebrook.yaml
//!   - specs/calcs/hydraulics/friction_factor_haaland.yaml
//!   - specs/calcs/hydraulics/friction_factor_swamee_jain.yaml
//!   - specs/calcs/hydraulics/orifice_flow.yaml
//!   - specs/calcs/hydraulics/pump_power.yaml
//!   - specs/calcs/hydraulics/reynolds_number.yaml
//!
//! Tables for the `hydraulics` namespace. Every namespace has its own generated
//! file, because a crate is the unit of compilation and a calculation must be able
//! to read its own bounds without depending on another namespace's crate. The
//! types these tables are built from are shared - see `azoth_core::spec`.
//!
//! These tables are what make the specs authoritative at runtime rather than
//! merely descriptive. Each calc reads its own range checks from here, so a
//! bound changed in a spec file changes the code's behaviour with no second
//! edit - and `cargo test` fails if the two ever disagree.
use azoth_core::{
    Band, CalcSpec, RangeCheck, Severity, SolverSpec, SpecCheck, TestCase, WarningCode,
};

/// Registry entry for `hydraulics.choked_flow_area`.
static CHOKED_FLOW_AREA_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "m_dot",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "flow is proportional to area, so a negative rate would give a negative area. Zero is allowed and correct: passing no mass needs no throat.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "P0",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "stagnation pressure enters under the square root, so zero gives no mass flux and would make the required area infinite; negative would make the root imaginary.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "rho0",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "stagnation density likewise enters under the square root and must be positive",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "k",
            min: Some(1.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "`k - 1` appears in a denominator in the exponent, so `k = 1` is a division by zero - and physically it is a substance whose specific heats are equal, which no gas is. `k = 1` is the boundary and is excluded.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "k",
            min: None,
            min_inclusive: true,
            max: Some(1.6666666666666667),
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "No known gas has an isentropic exponent above 5/3, which is the monatomic limit - one atom, three degrees of freedom, no vibrational contribution. A value above it is much more likely a lookup or a unit mistake than an exotic working fluid, and it is a warning rather than an error because `k` is a property: the relation evaluates perfectly well, and refusing would be less useful than saying so. This is the first warning bound in five calcs, and the difference is that `k` has a known range as a substance where the coefficients the others take do not.",
        },
    },
];

static CHOKED_FLOW_AREA_TESTS: &[TestCase] = &[
    TestCase {
        id: "different_in_every_input",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("m_dot", 2.5), ("P0", 500000.0), ("rho0", 6.0), ("k", 1.3)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("a", 0.0021631306940814474)],
        expected_vectors: &[],
    },
    TestCase {
        id: "agrees_with_the_stagnation_temperature_form",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[
            ("m_dot", 1.0),
            ("P0", 1000000.0),
            ("rho0", 11.614401858304298),
            ("k", 1.4),
        ],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("a", 0.00042853006428954324)],
        expected_vectors: &[],
    },
    TestCase {
        id: "monotonic",
        kind: "property",
        property: Some("monotonic"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.choked_flow_area`.
///
/// Public and addressable directly, so a calc can hold `&CHOKED_FLOW_AREA_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static CHOKED_FLOW_AREA_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.choked_flow_area",
    verification: "unverified",
    checks: CHOKED_FLOW_AREA_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "air_like_conditions_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[
            ("m_dot", 1.0),
            ("P0", 1000000.0),
            ("rho0", 10.0),
            ("k", 1.4),
        ],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("a", 0.00046182742602466937)],
        expected_vectors: &[],
    },
    tests: CHOKED_FLOW_AREA_TESTS,
};

/// Registry entry for `hydraulics.control_valve_cv`.
static CONTROL_VALVE_CV_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "Cv",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "flow is proportional to the coefficient, so a non-positive one gives no flow or negative flow - a number that is wrong rather than merely unusual. A closed valve is `Cv = 0` and is correctly a different question, since no coefficient describes it.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "dP",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a negative drop would mean flow in the other direction, which is a different calculation and not the one named here. Zero is allowed and correct: no pressure difference drives no flow.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "SG",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "specific gravity is a divisor inside the square root, so zero is singular and negative would make the root imaginary.",
        },
    },
];

static CONTROL_VALVE_CV_TESTS: &[TestCase] = &[
    TestCase {
        id: "realistic_bar_of_drop",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("Cv", 10.0), ("dP", 100000.0), ("SG", 1.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.00240271570956195)],
        expected_vectors: &[],
    },
    TestCase {
        id: "a_coefficient_of_one_passes_one_gallon_per_minute",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("Cv", 1.0), ("dP", 6894.757293168361), ("SG", 1.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 6.30901964e-05)],
        expected_vectors: &[],
    },
    TestCase {
        id: "denser_liquid_passes_less",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("Cv", 10.0), ("dP", 6894.757293168361), ("SG", 1.2)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.0005759320620951871)],
        expected_vectors: &[],
    },
    TestCase {
        id: "monotonic",
        kind: "property",
        property: Some("monotonic"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.control_valve_cv`.
///
/// Public and addressable directly, so a calc can hold `&CONTROL_VALVE_CV_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static CONTROL_VALVE_CV_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.control_valve_cv",
    verification: "unverified",
    checks: CONTROL_VALVE_CV_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "unit_drop_at_unit_specific_gravity_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("Cv", 10.0), ("dP", 6894.757293168361), ("SG", 1.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.0006309019640000001)],
        expected_vectors: &[],
    },
    tests: CONTROL_VALVE_CV_TESTS,
};

/// Registry entry for `hydraulics.crane_k_factors`.
static CRANE_K_FACTORS_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "f_t",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a non-positive friction factor gives a negative loss",
        },
    },
    SpecCheck {
        on_input: false,
        check: RangeCheck {
            quantity: "n_fittings",
            min: Some(1.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "An empty fitting list is a caller error rather than a physical state - the calculation would silently return zero loss, which reads as \"no fittings\" when the caller may have meant to pass some.",
        },
    },
];

static CRANE_K_FACTORS_TESTS: &[TestCase] = &[
    TestCase {
        id: "single_fitting_matches_coefficient",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-09,
        numbers: &[("f_t", 0.019)],
        lists: &[("fittings", &["90_elbow"])],
        vectors: &[],
        matrices: &[],
        expected: &[("k_total", 0.57)],
        expected_vectors: &[],
    },
    TestCase {
        id: "scaling_is_linear_in_f_t",
        kind: "property",
        property: Some("symmetry"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-09,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.crane_k_factors`.
///
/// Public and addressable directly, so a calc can hold `&CRANE_K_FACTORS_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static CRANE_K_FACTORS_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.crane_k_factors",
    verification: "unverified",
    checks: CRANE_K_FACTORS_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "elbow_and_gate_valve_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-09,
        numbers: &[("f_t", 0.018)],
        lists: &[("fittings", &["90_elbow", "gate_valve_open"])],
        vectors: &[],
        matrices: &[],
        expected: &[("k_total", 0.684)],
        expected_vectors: &[],
    },
    tests: CRANE_K_FACTORS_TESTS,
};

/// Registry entry for `hydraulics.darcy_weisbach`.
static DARCY_WEISBACH_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "f",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a non-positive friction factor is unphysical and would give a negative or zero loss",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "L",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "negative length is unphysical; zero length correctly gives zero drop",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "D",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "L/D is singular at D = 0",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "rho",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "pressure drop scales with density, which must be positive",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "v",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "speed is non-negative; the sign of the drop is not modelled",
        },
    },
    SpecCheck {
        on_input: false,
        check: RangeCheck {
            quantity: "re",
            min: Some(2000.0),
            min_inclusive: true,
            max: Some(4000.0),
            max_inclusive: true,
            equals: None,
            band: Band::Inside,
            severity: Severity::Warning,
            code: WarningCode::TransitionalFlow,
            rationale: "Transitional flow, where the friction factor - and therefore this pressure drop - is genuinely indeterminate rather than merely uncertain. The value returned is trustworthy arithmetic applied to an untrustworthy f. This check is only evaluable when mu is supplied; when it is not, the implementation must emit RANGE_CHECK_SKIPPED so an unchecked result is distinguishable from a checked one.",
        },
    },
];

static DARCY_WEISBACH_TESTS: &[TestCase] = &[
    TestCase {
        id: "crane_3_5",
        kind: "reference",
        property: None,
        status: "skipped",
        skip_reason: Some(
            "source_needed: the brief attributed this worked example to \"Crane TP-410 Example 3-5\", but the example could not be located or verified, and the expected value supplied with it was inconsistent with the stated equation by a factor of 10. Reproducing a worked example from the standard is also exactly what the project's copyright rule forbids. Skipped rather than guessed: the equivalent validation is covered by derived_worked_example and public_water_steel_example.",
        ),
        tolerance: 1e-09,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "perry_6_42",
        kind: "reference",
        property: None,
        status: "skipped",
        skip_reason: Some(
            "source_needed: \"Perry's 8th ed. Eq. 6-42\" could not be verified, and reproducing text or tables from Perry's is prohibited by the project's copyright rule. Skipped rather than fabricated.",
        ),
        tolerance: 1e-09,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "reynolds_reported_when_mu_supplied",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[
            ("f", 0.02),
            ("L", 100.0),
            ("D", 0.1),
            ("rho", 998.0),
            ("v", 1.5),
            ("mu", 0.001002),
        ],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("dp", 22455.0), ("re", 149401.19760479042)],
        expected_vectors: &[],
    },
    TestCase {
        id: "public_water_steel_example",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 0.02,
        numbers: &[
            ("f", 0.01884561),
            ("L", 100.0),
            ("D", 0.1),
            ("rho", 998.0),
            ("v", 1.768388),
        ],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("dp", 29600.0)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-09,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.darcy_weisbach`.
///
/// Public and addressable directly, so a calc can hold `&DARCY_WEISBACH_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static DARCY_WEISBACH_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.darcy_weisbach",
    verification: "unverified",
    checks: DARCY_WEISBACH_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "derived_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-09,
        numbers: &[
            ("f", 0.02),
            ("L", 100.0),
            ("D", 0.1),
            ("rho", 998.0),
            ("v", 1.5),
        ],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("dp", 22455.0)],
        expected_vectors: &[],
    },
    tests: DARCY_WEISBACH_TESTS,
};

/// Registry entry for `hydraulics.friction_factor_colebrook`.
static FRICTION_FACTOR_COLEBROOK_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "the 2.51/(Re*sqrt(f)) term is singular at Re = 0",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "negative roughness is unphysical",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(4000.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Below Re ~ 4000 the Colebrook equation is outside the range it was fitted to; it is a transition-region correlation for turbulent flow. The iteration still converges to a number, so this is a warning rather than an error, but the number should not be used. For laminar flow use f = 64/Re instead, which is a different equation and deliberately not part of this calc.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: None,
            min_inclusive: true,
            max: Some(0.05),
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Above epsilon/D ~ 0.05 the Moody/Colebrook framework is no longer meaningful, and this library also does not carry roughness data that large.",
        },
    },
];

static FRICTION_FACTOR_COLEBROOK_TESTS: &[TestCase] = &[
    TestCase {
        id: "smooth_pipe_agrees_with_textbook",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 0.001,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.0179898)],
        expected_vectors: &[],
    },
    TestCase {
        id: "fully_rough_limit",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[("re", 1000000000000.0), ("relative_roughness", 0.01)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.03790371)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.friction_factor_colebrook`.
///
/// Public and addressable directly, so a calc can hold `&FRICTION_FACTOR_COLEBROOK_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static FRICTION_FACTOR_COLEBROOK_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.friction_factor_colebrook",
    verification: "verified",
    checks: FRICTION_FACTOR_COLEBROOK_CHECKS,
    solver: Some(SolverSpec {
        kind: "fixed_point",
        tolerance: 1e-12,
        max_iterations: 100,
        initial_guess: Some(0.02),
        convergence: "relative",
    }),
    worked_example: TestCase {
        id: "commercial_steel_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.00046)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.020162032)],
        expected_vectors: &[],
    },
    tests: FRICTION_FACTOR_COLEBROOK_TESTS,
};

/// Registry entry for `hydraulics.friction_factor_haaland`.
static FRICTION_FACTOR_HAALAND_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "the 6.9/Re term is singular at Re = 0",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "negative roughness is unphysical",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(4000.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Below Re ~ 4000 the equation is outside turbulent pipe flow, which is the regime it approximates within Colebrook. It still returns a finite number - `log10` of a positive argument is defined everywhere the hard bounds allow - so this is a warning rather than an error, but that number is not covered by the accuracy claim. For laminar flow use f = 64/Re, which is a different equation and deliberately not part of this calc. This bound is the framework boundary already applied to Colebrook in this registry, not a figure read from Haaland's paper. See `verification`.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: None,
            min_inclusive: true,
            max: Some(0.05),
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Above epsilon/D ~ 0.05 the Moody framework is no longer meaningful, and this library does not carry roughness data that large. The bound is inclusive because 0.05 is itself still inside the framework; it is the same bound and the same inclusive flag the Colebrook spec uses. See `verification` - it is the framework's bound, not Haaland's.",
        },
    },
];

static FRICTION_FACTOR_HAALAND_TESTS: &[TestCase] = &[
    TestCase {
        id: "agrees_with_colebrook_within_two_percent",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 0.02,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.00046)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.020162032)],
        expected_vectors: &[],
    },
    TestCase {
        id: "smooth_pipe_limit",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.017824939200764653)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.friction_factor_haaland`.
///
/// Public and addressable directly, so a calc can hold `&FRICTION_FACTOR_HAALAND_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static FRICTION_FACTOR_HAALAND_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.friction_factor_haaland",
    verification: "unverified",
    checks: FRICTION_FACTOR_HAALAND_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "commercial_steel_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.00046)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.0198980585)],
        expected_vectors: &[],
    },
    tests: FRICTION_FACTOR_HAALAND_TESTS,
};

/// Registry entry for `hydraulics.friction_factor_swamee_jain`.
static FRICTION_FACTOR_SWAMEE_JAIN_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "Re**0.9 is zero at Re = 0 and the 5.74/Re**0.9 term is singular",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "negative roughness is unphysical",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: Some(5000.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Below Re = 5000 the equation is outside the range stated in the original paper, where the approximation was fitted. It still returns a number, so this is a warning rather than an error, but that number is not covered by the paper's accuracy claim.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "re",
            min: None,
            min_inclusive: true,
            max: Some(100000000.0),
            max_inclusive: false,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "upper Reynolds-number bound stated in the original paper",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: Some(1e-06),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "lower relative-roughness bound stated in the original paper",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "relative_roughness",
            min: None,
            min_inclusive: true,
            max: Some(0.01),
            max_inclusive: false,
            equals: None,
            band: Band::Outside,
            severity: Severity::Warning,
            code: WarningCode::OutOfValidRange,
            rationale: "Upper relative-roughness bound stated in the original paper (10**-2). NOTE: the literature is not consistent here - several widely used references quote the upper bound as 5e-2 instead, and a few quote 4e-5 as the lower bound. This spec encodes the original paper's figures. The value last listed in `references` is the comparison against Colebrook that justifies the loose tolerance on the agreement test.",
        },
    },
];

static FRICTION_FACTOR_SWAMEE_JAIN_TESTS: &[TestCase] = &[
    TestCase {
        id: "agrees_with_colebrook_within_one_percent",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 0.01,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.00046)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.020162032)],
        expected_vectors: &[],
    },
    TestCase {
        id: "exact_smooth_pipe_limit",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 0.0001,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.01786257789)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.friction_factor_swamee_jain`.
///
/// Public and addressable directly, so a calc can hold `&FRICTION_FACTOR_SWAMEE_JAIN_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static FRICTION_FACTOR_SWAMEE_JAIN_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.friction_factor_swamee_jain",
    verification: "verified",
    checks: FRICTION_FACTOR_SWAMEE_JAIN_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "commercial_steel_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-06,
        numbers: &[("re", 100000.0), ("relative_roughness", 0.00046)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("f", 0.0202400309)],
        expected_vectors: &[],
    },
    tests: FRICTION_FACTOR_SWAMEE_JAIN_TESTS,
};

/// Registry entry for `hydraulics.orifice_flow`.
static ORIFICE_FLOW_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "d",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "the bore is squared, so a zero bore would give zero area and zero flow, and a negative one would give a positive area from a nonsensical input. Both are caller errors rather than physical states.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "dP",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a negative difference would mean flow in the other direction, which is a different calculation and not the one named here. Zero is allowed and correct: no pressure difference drives no flow.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "rho",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "density is a divisor inside the square root, so zero is singular and negative would make the root imaginary.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "Cd",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a non-positive coefficient would give a flow that is zero or in the wrong direction - wrong rather than merely unusual.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "Cd",
            min: None,
            min_inclusive: true,
            max: Some(1.0),
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "the ideal, lossless orifice passes exactly the ideal flow, so a coefficient above 1 would claim more flow than an obstruction of that area can pass at that pressure difference. That is not an uncertainty to warn about but a violation of the relation, so it is rejected. The bound is inclusive because 1 is the ideal limit and a legitimate one.",
        },
    },
];

static ORIFICE_FLOW_TESTS: &[TestCase] = &[
    TestCase {
        id: "larger_bore_and_lower_differential",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("d", 100.0), ("dP", 10000.0), ("rho", 1000.0), ("Cd", 0.61)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.021425684929674216)],
        expected_vectors: &[],
    },
    TestCase {
        id: "an_ideal_orifice_passes_the_ideal_flow",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("d", 50.0), ("dP", 25000.0), ("rho", 998.0), ("Cd", 1.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.013897914051711287)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "monotonic",
        kind: "property",
        property: Some("monotonic"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.orifice_flow`.
///
/// Public and addressable directly, so a calc can hold `&ORIFICE_FLOW_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static ORIFICE_FLOW_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.orifice_flow",
    verification: "unverified",
    checks: ORIFICE_FLOW_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "water_through_a_50mm_orifice_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("d", 50.0), ("dP", 25000.0), ("rho", 998.0), ("Cd", 0.62)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("q", 0.008616706712060997)],
        expected_vectors: &[],
    },
    tests: ORIFICE_FLOW_TESTS,
};

/// Registry entry for `hydraulics.pump_power`.
static PUMP_POWER_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "rho",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "power scales linearly with density, so a non-positive density would give a zero or negative shaft power - a number that is wrong rather than merely out of range.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "q",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "flow is a magnitude and the sign is not modelled, so a negative value is a caller error rather than reversed flow. Zero is allowed and correct: no flow moves no fluid, so no power is required.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "H",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a negative head means the machine is extracting energy from the fluid, which is a turbine and a different calculation. Zero is allowed and gives zero power: developing no head needs no power.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "eta",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "`eta` is a divisor, so zero is singular - and physically it would mean a pump requiring infinite shaft power to deliver finite hydraulic power.",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "eta",
            min: None,
            min_inclusive: true,
            max: Some(1.0),
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "An efficiency above 1 would mean more hydraulic power out than shaft power in. That is not an engineering uncertainty to warn about but a violation of the first law, so it is rejected rather than flagged. The bound is inclusive because 1 is the ideal pump: lossless, and a legitimate limiting case.",
        },
    },
];

static PUMP_POWER_TESTS: &[TestCase] = &[
    TestCase {
        id: "denser_fluid_different_operating_point",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("rho", 1200.0), ("q", 0.05), ("H", 12.0), ("eta", 0.6)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("power", 11767.98)],
        expected_vectors: &[],
    },
    TestCase {
        id: "an_ideal_pump_delivers_the_hydraulic_power_exactly",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("rho", 998.0), ("q", 0.01), ("H", 30.0), ("eta", 1.0)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("power", 2936.11101)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
    TestCase {
        id: "monotonic",
        kind: "property",
        property: Some("monotonic"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.pump_power`.
///
/// Public and addressable directly, so a calc can hold `&PUMP_POWER_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static PUMP_POWER_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.pump_power",
    verification: "unverified",
    checks: PUMP_POWER_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "water_10_litres_per_second_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("rho", 998.0), ("q", 0.01), ("H", 30.0), ("eta", 0.75)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("power", 3914.81468)],
        expected_vectors: &[],
    },
    tests: PUMP_POWER_TESTS,
};

/// Registry entry for `hydraulics.reynolds_number`.
static REYNOLDS_NUMBER_CHECKS: &[SpecCheck] = &[
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "rho",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "density must be positive; it divides into nothing here but a non-positive value is unphysical",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "D",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "a zero diameter makes the Reynolds number zero regardless of flow",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "mu",
            min: Some(0.0),
            min_inclusive: false,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "zero viscosity makes the Reynolds number undefined rather than large",
        },
    },
    SpecCheck {
        on_input: true,
        check: RangeCheck {
            quantity: "v",
            min: Some(0.0),
            min_inclusive: true,
            max: None,
            max_inclusive: true,
            equals: None,
            band: Band::Outside,
            severity: Severity::Error,
            code: WarningCode::OutOfValidRange,
            rationale: "speed is non-negative; flow direction is not modelled by this calc",
        },
    },
    SpecCheck {
        on_input: false,
        check: RangeCheck {
            quantity: "re",
            min: Some(2000.0),
            min_inclusive: true,
            max: Some(4000.0),
            max_inclusive: true,
            equals: None,
            band: Band::Inside,
            severity: Severity::Warning,
            code: WarningCode::TransitionalFlow,
            rationale: "Transitional flow. Downstream friction-factor correlations are indeterminate in this band and the two branches disagree by design, so any pressure drop computed from this Reynolds number should be treated as unreliable. This is a warning rather than an error because the Reynolds number itself is perfectly well defined here - it is the correlations that are not.",
        },
    },
];

static REYNOLDS_NUMBER_TESTS: &[TestCase] = &[
    TestCase {
        id: "transitional_warning",
        kind: "reference",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("rho", 1000.0), ("v", 0.1), ("D", 0.05), ("mu", 0.0015)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("re", 3333.3333333333335)],
        expected_vectors: &[],
    },
    TestCase {
        id: "round_trip_units",
        kind: "property",
        property: Some("unit_round_trip"),
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[],
        expected_vectors: &[],
    },
];

/// Registered spec for `hydraulics.reynolds_number`.
///
/// Public and addressable directly, so a calc can hold `&REYNOLDS_NUMBER_SPEC` with no
/// lookup and no failure path. A calc whose spec is missing is a build-time
/// invariant, not a runtime condition, and this shape makes it unrepresentable
/// rather than something to handle.
pub static REYNOLDS_NUMBER_SPEC: CalcSpec = CalcSpec {
    id: "hydraulics.reynolds_number",
    verification: "unverified",
    checks: REYNOLDS_NUMBER_CHECKS,
    solver: None,
    worked_example: TestCase {
        id: "water_pipe_worked_example",
        kind: "worked_example",
        property: None,
        status: "active",
        skip_reason: None,
        tolerance: 1e-12,
        numbers: &[("rho", 998.0), ("v", 1.5), ("D", 0.1), ("mu", 0.001002)],
        lists: &[],
        vectors: &[],
        matrices: &[],
        expected: &[("re", 149401.19760479042)],
        expected_vectors: &[],
    },
    tests: REYNOLDS_NUMBER_TESTS,
};

/// Every calculation in the registry, sorted by id.
static ALL_SPECS: &[&CalcSpec] = &[
    &CHOKED_FLOW_AREA_SPEC,
    &CONTROL_VALVE_CV_SPEC,
    &CRANE_K_FACTORS_SPEC,
    &DARCY_WEISBACH_SPEC,
    &FRICTION_FACTOR_COLEBROOK_SPEC,
    &FRICTION_FACTOR_HAALAND_SPEC,
    &FRICTION_FACTOR_SWAMEE_JAIN_SPEC,
    &ORIFICE_FLOW_SPEC,
    &PUMP_POWER_SPEC,
    &REYNOLDS_NUMBER_SPEC,
];

/// All specs, in a stable order.
#[must_use]
pub fn specs() -> &'static [&'static CalcSpec] {
    ALL_SPECS
}

/// Look up a spec by id.
#[must_use]
pub fn spec(id: &str) -> Option<&'static CalcSpec> {
    ALL_SPECS.iter().copied().find(|s| s.id == id)
}
